# Notion-to-Note Article AutoDrafter Environment Variables
# Copy this file to .env and fill in your values

# OpenAI API Key
OPENAI_API_KEY=sk-xxxxx

# OpenAI Model (optional, defaults to gpt-4o-mini)
# Recommended models:
#   - gpt-4o      : 最高品質（推奨）- 高品質な記事を求める場合はこれを選択
#   - gpt-4o-mini : コスパ最強。十分な性能だが、品質重視の場合は gpt-4o を推奨
#   - gpt-3.5-turbo : 最安だが品質は劣る
OPENAI_MODEL=gpt-4o

# Notion Integration Token
NOTION_TOKEN=secret_xxxxx

# Notion Database ID (記事用データベースのID)
# データベースURLの末尾の32文字
NOTION_DATABASE_ID=xxxxx

# note.com Session State (optional, defaults to ./note-state.json)
# NOTE_STATE_FILE=./note-state.json
