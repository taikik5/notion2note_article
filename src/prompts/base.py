"""
Base prompt containing note.com markdown rules.
"""

NOTE_MARKDOWN_RULES = """# 重要: note.comのマークダウン対応ルール

note.comは以下のマークダウン記法のみ対応しています：
- 大見出し: `## ` （シャープ2つ + 半角スペース）
- 小見出し: `### ` （シャープ3つ + 半角スペース）
- 箇条書き: `- ` （ハイフン + 半角スペース）
- 太字: `**テキスト**`

【絶対に守ること】
1. `##` や `###` の後には必ず半角スペースを1つ入れる
2. `#`（h1）は使わない（note.comでは対応外）
3. `####` 以上は使わない（note.comでは対応外）
4. 箇条書きは `- ` を使う（`*` や `•` ではなく）
5. 番号付きリスト（1. 2. 3.）は使わない（note.comでは対応外）

【出力形式】
- タイトルは1行目に見出し記号なしで出力（タイトルは30文字以内）
- 本文はマークダウン形式
- ```markdown などのコードブロックで囲まない
"""
